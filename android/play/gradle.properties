org.gradle.jvmargs=-Xmx2560m
android.useAndroidX=true
# No Kotlin/Java code in this module, but AGP's application plugin still
# reads this; keep it explicit rather than relying on the AGP default.
android.nonTransitiveRClass=true
